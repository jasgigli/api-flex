{
  "name": "api-flex",
  "version": "1.0.2", 
  "description": "A powerful, easy-to-use library for seamless API integration with built-in retries, caching, and error handling.",
  "main": "src/index.js", 
  "module": "src/index.js", 
  "types": "src/index.d.ts", 
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "prepublishOnly": "npm run lint",
    "prepare": "npm run build", 
    "build": "echo 'No build step required'", 
    "version": "npm version patch -m 'Upgrade to version %s'"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "eslint": "^8.44.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
